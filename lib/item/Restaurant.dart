class Restaurant {
  int restaurantSeq;
  String title;
  String category;
  double starRating;
  int starCnt;
  String address;
  String number;
  double longitude;
  double latitude;

  Restaurant(this.restaurantSeq, this.title, this.category, this.starRating,
      this.starCnt, this.address, this.number, this.longitude, this.latitude);
}
